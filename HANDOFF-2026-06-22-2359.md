# Handoff: Δ₁-definability of induction scheme — forward-direction machinery COMPLETE

**Date**: 2026-06-22 23:59 · **Branch**: `feat/induction-scheme-delta1` · **HEAD**: `894da27`

## 🎯 Goal (unchanged)
Discharge `PA_delta1Definable : 𝗣𝗔.Δ₁` and `ISigma1_delta1Definable : 𝗜𝚺₁.Δ₁` by PROVING them in
`Foundation/FirstOrder/Incompleteness/InductionSchemeDelta1.lean` (done-when target, host stops when
sorry-free). Win = `#print axioms` on both = `[propext, Classical.choice, Quot.sound]`.
`𝗣𝗔⁻.Δ₁` (B1) done; both headlines assembled via `Theory.Δ₁.add`. Only open obligations: the 2 crux
instances `InductionScheme.delta1_univ` / `delta1_sigma1` (sorries at lines ~331/335).

## 🔑 KEY CORRECTION THIS LAP (don't repeat the old mistake)
The prior handoff was WRONG: `!φ t` in **formula** position is `φ ⇜ ![t]` = `Rew.substs`, **NOT**
`embSubsts` (BinderNotation.lean:419–428 → `$φ ⇜ $v`). So `⌜succInd φ⌝` decomposes with the
**already-present** `typed_quote_substs` simp lemma. The whole "build `typed_quote_embSubsts`"
task is VOID. Verified: `typed_quote_succInd`, `succInd_eq` in the file.

## ✅ DONE this lap (all sorry-free, axiom-clean, full Foundation builds GREEN — 1194 jobs)
The entire **forward-direction code machinery** for the recognizer is now proven:
- `quote_univCl` / `quote_univCl'` / **`quote_univCl_eq`**: `⌜univCl ψ⌝ = qqAlls ⌜fixitr 0 (fvSup ψ) ▹ ψ⌝ (0 + fvSup ψ)`.
- **`quote_subst_fvar_fixitr`**: `⌜(fixitr 0 (fvSup φ) ▹ φ) ⇜ (&·)⌝ = ⌜φ⌝` (meta inversion, via `subst_comp_fixitr`).
- `succInd_eq`, **`typed_quote_succInd`**: `⌜succInd φ⌝` decomposes into typed `subst`/`🡒`/`∀⁰`.
- **`indBody`** (typed reconstruction of the succInd shape from core `K`) + **`indBody_quote`**: `indBody ⌜ψ⌝ = ⌜succInd ψ⌝`.
- **`fvarVec`** (new internal `𝚺₁` function `⟨^&0..^&(k-1)⟩`): `fvarVec_zero/succ`, `fvarVecDef` +
  `𝚺₁-Function₁` defined/definable instances, `len_fvarVec`, `nth_fvarVec`, `fvarVec_val_eq`.
- **`subst_fvarVec_quote`**: `subst ℒₒᵣ (fvarVec (fvSup φ)) ⌜fixitr ▹ φ⌝ = ⌜φ⌝` (raw closure inversion).

⇒ For a member `p = ⌜univCl(succInd ψ)⌝`: `p = qqAlls b m` with `m = 0+fvSup`, `b = ⌜fixitr ▹ succInd ψ⌝`,
and `subst (fvarVec m) b = ⌜succInd ψ⌝ = indBody ⌜ψ⌝` — ALL verified. The recognizer's forward
soundness is one short composition away.

## 🧠 THE RECOGNIZER (refined — needs NO internal `fvSup`/`fixitr`; see PENDING_WORK.md ⭐)
For `C = Set.univ`:
```
ch(p) := ∃ m ≤ p, ∃ b ≤ p, ∃ K ≤ p,
   p = qqAlls b m  ∧  IsUFormula ℒₒᵣ b ∧ shift ℒₒᵣ b = b  ∧  bv ℒₒᵣ b = m
 ∧ IsSemiformula ℒₒᵣ 1 K  ∧  subst (fvarVec m) b = (indBody K).val
```
`bv b = m` PINS `m = fvSup` (kills over-recognition by padding leading ∀s — without an internal
fvSup function). `indBody K` recovers the succInd shape; `K = ⌜ψ⌝`. For `C = Hierarchy 𝚺 1`, add an
internal "K is Σ₁" predicate as the side condition.

## 🎬 NEXT actions (in order; everything below is "grind", math content is mostly done)
1. **meta+internal `bv` pin lemma**: `bv ℒₒᵣ (⌜fixitr 0 (fvSup χ) ▹ χ⌝ : V) = fvSup χ`. Needs (a) an
   internal-`bv`↔meta bridge (no `bv_quote` lemma exists yet — likely prove by `IsSemiformula`/quote
   induction, or relate via `bv_all`/`bv_qqAlls` already proven), and (b) meta "sup attained":
   `&(fvSup χ - 1)` occurs in χ (from `fvSup`/`freeVariables` defs in `Basic/Syntax/Rew.lean`).
2. **extract `indBody.val : V → V` as a `𝚺₁`-graph** function (pattern: `Functions.lean` `neg`/`subst`
   expose `…Graph`+`defined`). `indBody` is a typed composite of definable ops, so its `.val` is Σ₁.
3. **assemble `ch : 𝚫₁.Semisentence 1`** with `HierarchySymbol.Semiformula` combinators
   (`bex`/`and` + graph-equalities). Prove `DefinedPred (V:=V) R ch` (template: `qqAllsDef`,
   `fvarVecDef`, `IsSemiformula`). `Defined` (Δ-[m]) CARRIES `ProperOn V` (Definable.lean:186) ⇒
   `isDelta1 := ProvablyProperOn.ofProperOn _ (fun V _ _ ↦ Defined.proper)` is FREE.
   ⚠ `Theory.Δ₁.ch` needs a CONCRETE semisentence — `Definable` is a `Prop`, formula NOT extractable.
4. **`mem_iff` over ℕ**. Forward: compose the DONE bridges above. Backward: `IsSemiformula.sound`
   gives `ψ` from `K`; the {FV-free, bv=m}≅{fvSup=m} bijection (inverse `fixitr`) + `subst_fvarVec_quote`
   pin `b`, `m`; then `quote_univCl_eq` closes `p = ⌜univCl(succInd ψ)⌝`.
5. Package `delta1_univ` (clears PA). Then `delta1_sigma1`: internal Σ₁-formula predicate for `Cᵢ`.

## ⚠️ Gotchas (verified this lap)
- `rw [Sentence.quote_def]`/`quote_allClosure` hit `Language.Encodable ?m` metavar — use `show … from rfl`
  (quote_def IS rfl) or pass `(V := V)`/explicit args instead of bare `rw`.
- `definability` fails on motives with an UNBOUNDED `∀` (handoff-known). `∀ i < k, …` is BOUNDED & fine
  (see `nth_fvarVec`). Don't `generalizing` an unbounded index.
- For PR.Construction `succ_defined` with a bounded ∃ + multiple `!…Def`, drop the `<⁺` bound and use
  unbounded `∃ f`; `simp [blueprint]` then closes it (avoids proving the witness bound).
- nonstandard indices: `eq_nat_of_lt_nat : x < ↑n → ∃ m:ℕ, x = ↑m` (PeanoMinus/Basic.lean:319).
- typed-vector code↔nth: `SemitermVec.val_nth_eq`, `nth_ext`, `matrixToVec_nth`, `Semiterm.typed_quote_fvar`.

## 📁 Key files
- `Foundation/FirstOrder/Incompleteness/InductionSchemeDelta1.lean` — deliverable (10 new lemmas; 2 crux sorries).
- `PENDING_WORK.md` — ⭐ REFINED RECOGNIZER section (authoritative design; DONE/⬜ checklist).
- Internal API: `Bootstrapping/Syntax/Formula/{Basic,Functions,Coding,Typed}.lean`, `HFS/Vec.lean` (concat, repeatVec).

---
**→ Next session: start at NEXT action #1 (`bv` pin lemma) or #2/#3 (assembly). The hard math
(closure inversion both directions, succInd reconstruction, fvarVec) is DONE and verified — what
remains is the internal-`bv` bridge + mechanical Δ₁-formula assembly + mem_iff packaging.**
