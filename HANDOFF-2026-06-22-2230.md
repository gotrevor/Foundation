# Handoff: Δ₁-definability of the induction scheme — closure-half proven

**Date**: 2026-06-22 22:30 · **Branch**: `feat/induction-scheme-delta1` · **HEAD**: `7633ca6`

## 🎯 What we're doing
Discharge Foundation's two `axiom`s `PA_delta1Definable : 𝗣𝗔.Δ₁` and
`ISigma1_delta1Definable : 𝗜𝚺₁.Δ₁` by PROVING them, in the done-when target
`Foundation/FirstOrder/Incompleteness/InductionSchemeDelta1.lean` (host stops when it's
sorry-free). Win = `#print axioms` on both = `[propext, Classical.choice, Quot.sound]`.
Route: `𝗣𝗔 = 𝗣𝗔⁻ + InductionScheme ℒₒᵣ Set.univ`, `𝗜𝚺₁ = 𝗣𝗔⁻ + InductionScheme ℒₒᵣ (Hierarchy 𝚺 1)`;
`Theory.Δ₁.{ofFinite,add}` reduce both to the one obligation `(InductionScheme ℒₒᵣ C).Δ₁`.

## 🧠 Context to carry forward
- **The whole crux is `(InductionScheme ℒₒᵣ C).Δ₁`** — a `Theory.Δ₁` record `⟨ch, mem_iff, isDelta1⟩`.
  `InductionScheme C = { univCl(succInd ψ) | C ψ }`. The recognizer must be Δ₁ over codes.
- **Recognizer = DECODE (chosen over RECONSTRUCT) because it AVOIDS building internal `fixitr`.**
  Key meta facts (`Basic/Syntax/Rew.lean`): `univCl'(succInd ψ) = ∀⁰* b`, `b = fixitr 0 m ▹ succInd ψ`,
  `m = fvSup ψ`; `b` is freevar-free (= internal `IsFVFree`, Σ₁) and `b ⇜ (&·) = succInd ψ`
  (`subst_comp_fixitr`). So the closure is invertible via the ALREADY-PROVEN internal `subst` — no
  need to build a free→bound rewrite. This was the lap's main design win; don't re-litigate it.
- **`succInd` desugaring (decoded this lap)**: in the `“…”` macro `!φ t₁…tₙ` = `Rew.embSubsts ![t…] ▹ φ`
  (`Basic/BinderNotation.lean:419`), and `embSubsts v = Rew.bind v Empty.elim` (`Syntax/Predicate/Rew.lean:134`).
  So `succInd φ = (embSubsts ![0] ▹ φ) 🡒 ((∀⁰((embSubsts ![#0]▹φ) 🡒 (embSubsts ![#0+1]▹φ))) 🡒 ∀⁰(embSubsts ![#0]▹φ))`.
- **Why deferred `IsSemiformula.qqAlls` first attempt failed**: inducting with `generalizing n`
  puts an UNBOUNDED `∀ n` in the `ISigma1.sigma1_succ_induction` motive → not Σ₁ → `definability`
  fails. Fix used: route through `bv` (`isUFormula_qqAlls`, `bv_qqAlls`) — motives with no
  unbounded quantifier. Reuse this pattern for any qqAlls-style structural lemma.
- **Tactic gotcha**: in this `V`, `add_le_add_right h 1` elaborated to `1 + a ≤ 1 + b` (wrong shape);
  use `add_le_add h (le_refl 1)` instead.
- Aristotle is a POOR fit (mathlib-tuned; this API is bespoke `qq*`/`IsSemiformula`). Stay local.
- Upstream FFL (local clone `~/src/Foundation` `origin/master`) still has the axioms — nothing to port.

## ✅ State (all verified this lap; full `Foundation` builds GREEN)
- **B1 done, axiom-clean**: `PeanoMinus.delta1 : 𝗣𝗔⁻.Δ₁ := Theory.Δ₁.ofFinite _ PeanoMinus.finite`.
- **Both axioms DELETED** from `Examples.lean` → proven instances. `#print axioms` on both headlines
  = only `sorryAx` + standard three (no rogue axioms). The 2 sorries are the crux instances.
- **Crux part (a) — internal universal closure — COMPLETE, sorry-free, axiom-clean**:
  `qqAlls p k = ^∀^[k] p` (Σ₁ via `qqAllsDef`) + `qqAlls_all`, `qqAlls_succ'`, `le_qqAll`,
  `le_qqAlls`, `succ_le_qqAll`, `index_le_qqAlls`, `isUFormula_qqAlls`, `bv_qqAlls`,
  `IsSemiformula.qqAlls`, and headline `quote_allClosure : ⌜∀⁰* φ⌝ = qqAlls ⌜φ⌝ n` (verified
  axiom-clean via `#print axioms`).
- 11 commits this lap, HEAD `7633ca6`, working tree clean.

## 🎬 Next actions
1. **Build `typed_quote_embSubsts`** (the bridge that unblocks everything): prove
   `⌜Rew.embSubsts w ▹ φ⌝ = Bootstrapping.Semiformula.subst (fun i ↦ ⌜w i⌝) ⌜φ⌝`, modeled on
   `typed_quote_substs` (`Formula/Coding.lean`). FIRST check if `embSubsts`/`bind` already has a
   quote lemma. Watch the fvar-type subtlety (`embSubsts` source is `Empty`; `succInd`'s φ is over ℕ).
2. Build `succIndBody` (typed `Bootstrapping.Semiformula` layer) from the desugaring above; prove
   `⌜succInd ψ⌝ = (succIndBody ⌜ψ⌝).val` via the `typed_quote_*` simp set.
3. Assemble `ch` for `C = Set.univ` (side cond `⊤`) using the DECODE shape (see `PENDING_WORK.md`
   recognizer); discharge `mem_iff` (fwd via `quote_allClosure` + `subst_comp_fixitr`) and
   `isDelta1`/`ProperOn` (model on `Δ₁Class.defined`). Finish `delta1_univ` → clears PA.
4. Then `C = Hierarchy 𝚺 1`: add internal "ψ is Σ₁" predicate. Clears IΣ₁.
5. Commit each green; re-pin downstream Goodstein dep; offer branch upstream to FFL.

## ⚠️ Gotchas
- Do NOT add any `axiom`/`sorry` to close the crux — win is zero of both on the final instances.
- `definability` fails on unbounded-∀ motives (see Context). `lake build` of the deliverable file is
  the verification; never claim green unseen.

## 📁 Key files
- `Foundation/FirstOrder/Incompleteness/InductionSchemeDelta1.lean` — deliverable (qqAlls done; 2 crux sorries).
- `PENDING_WORK.md` — full DECODE design, recognizer shape, Paths A/B/C, API inventory.
- `HANDOFF-2026-06-22.md` — prior detailed handoff (block decomposition + line refs).
- `Foundation/FirstOrder/Bootstrapping/Syntax/Formula/{Basic,Functions,Coding,Typed}.lean` — internal API.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then start at Next action #1: build `typed_quote_embSubsts`.**
