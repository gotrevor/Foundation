# Handoff: Δ₁-definability — **PA HEADLINE DONE (axiom-clean)**; IΣ₁ scaffolded

**Date**: 2026-06-23 (lap d) · **Branch**: `feat/induction-scheme-delta1` · **HEAD**: see `git log`
**Build**: 🟢 green — `lake build Foundation.FirstOrder.Incompleteness.InductionSchemeDelta1`
(and `…Examples`). `src/` deliverable has exactly **1 sorry**: `delta1_sigma1` (the IΣ₁ case).

## 🎯 Goal
Discharge `PA_delta1Definable : 𝗣𝗔.Δ₁` and `ISigma1_delta1Definable : 𝗜𝚺₁.Δ₁` by PROVING them.
Win = `#print axioms` on both = `[propext, Classical.choice, Quot.sound]`.

## ✅ THIS LAP — `PA_delta1Definable` PROVEN axiom-clean (headline #1 of 2)
**Verified:** `#print axioms PA_delta1Definable = [propext, Classical.choice, Quot.sound]`.

The multi-lap **size race** (`⌜ψ⌝ ≤ ⌜univCl'(succInd ψ)⌝`, a brutal Nat-pairing inequality with
leaf-shrink amplification through `fixitr`) was **dissolved, not fought**, by restructuring `chUniv`:
- Bound the recovered core `K` by the **functionally-pinned** `s := subst (fvarVec m) b` (already an
  unbounded-but-functional `∃ s` in the recognizer) instead of by the input `p`.
- Π-form stays Π₁: keep `indBodyVal` in functional form `∀ ib, indBodyValGraph ib K → s = ib`,
  `K` bounded by `s` (`∃ K < s+1` = bounded-∃ of Π₁ = Π₁).
- Backward obligation flips to `⌜ψ⌝ ≤ indBodyVal ⌜ψ⌝ = ⌜succInd ψ⌝` — the **clean half**, new lemma
  `le_indBodyVal (k : V) : k ≤ indBodyVal k` (`le_qqAll` + two `lt_or_right`; prove the `<`→`≤` cast
  in a **generic-V helper** to dodge the ORingStructure/Preorder LT diamond — `.le`/`le_of_lt` at
  `V=ℕ` diamonds out: foundation `≤` vs `Nat.le`).
- `InductionUnivR.defined` carried the quantifier reorder under the SAME `simp` — no change.

## 🎬 NEXT — only `delta1_sigma1` (IΣ₁) remains; the deep arithmetization
Need an internal `Δ₁` predicate `IsSigma1 : V → Prop` with `IsSigma1 ⌜ψ⌝ ↔ Hierarchy 𝚺 1 ψ`, then a
`chSigma1` recognizer = `chUniv` + one `isSigma1` conjunct. **A full draft is in
`wip/IsSigma1-draft.lean`** (NOT built) with the structure done and blockers listed. Execute it:

1. **Characterization (CONFIRMED, `Hierarchy.sigma₁_induction'`):** over ℒₒᵣ, `Σ₁` = atoms
   (`=,≠,<,≮,⊤,⊥`) closed under `∧,∨`, (unbounded) `∃`, and **bounded-∀** `∀⁰[“#0 < bShift t”] φ`
   (body = `qqAll (qqOr (qqNLT (qqBvar 0) u) q)`, `u = termBShift t`).
2. **`IsSigma1` fixpoint** (template `FormalizedFormula`, `Formula/Basic.lean:201-282`): draft has
   `Phi`/`phi_iff`/`blueprint` (σ/π)/`construction`/`StrongFinite`/`Δ₁ instance`. **Blockers to fix
   before it compiles** (all in the wip header):
   - SOUNDNESS: guard must be `∃ t, IsUTerm ℒₒᵣ t ∧ u = termBShift ℒₒᵣ t` (add `isUTerm.σ/.π` to
     blueprint) — else over-accepts junk `t`.
   - sub-lemma `le_termBShift : IsUTerm ℒₒᵣ t → t ≤ termBShift ℒₒᵣ t` (via `IsUTerm.induction`;
     bvar `^#z ≤ ^#(z+1)` pair-mono, fvar refl, func needs vec-mono — ~30 lines).
   - sub-lemma `lt_qqNLT_right : y < qqNLT x y` (mirror `lt_qqLT_right` with `nth_lt_qqNRel_of_lt`).
   - tune the `defined` `simp` (add `isUTerm.σ/.π`, `termBShift`/`qqNLT` graph `.df.iff`).
3. **Correctness `IsSigma1 ⌜ψ⌝ ↔ Hierarchy 𝚺 1 ψ`:** (⟸) `sigma₁_induction'`; (⟹)
   `IsSemiformula.sigma1_structural_induction` (`Formula/Basic.lean:1352`). Bounded-∀ uses
   `typed_quote_bShift` / `Rew.positive_iff`.
4. **Integrate:** `chSigma1` = `chUniv` + `!(isSigma1 ℒₒᵣ).sigma/.pi K`; redo `.defined` (same simp);
   `chSigma1_mem_iff` = copy of `chUniv_mem_iff` + side condition; `delta1_sigma1` mirrors `delta1_univ`.

Est: ~2-3 laps. The positivity check being **Δ₁** (bounded-∃ since `termBShift` grows) is the key
unlock — no separate "is-positive" recursion needed.

## 📁 Pointers
- `PENDING_WORK.md` — newest 3 sections: PA dissolution + full IΣ₁ blueprint + positivity resolution.
- `wip/IsSigma1-draft.lean` — drafted fixpoint + blocker list (promote to deliverable when green).
- `DIRECTION.md` — frozen route. Deliverable: `…/InductionSchemeDelta1.lean` (done-when target).
- Aristotle: poor fit (bespoke Foundation API) — stay local.

**Laps to finish: ~2-3 for IΣ₁. PA (headline #1) is COMPLETE and axiom-clean.**
