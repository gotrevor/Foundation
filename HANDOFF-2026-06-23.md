# Handoff: Δ₁-definability crux — KEYSTONE CRACKED (bv-pin bridge proven)

**Date**: 2026-06-23 (lap end) · **Branch**: `feat/induction-scheme-delta1` · **HEAD**: `dab8b39`
**Build**: 🟢 green — `lake build Foundation.FirstOrder.Incompleteness.InductionSchemeDelta1`.
`src/` has exactly the **2 designated crux sorries** (`delta1_univ`, `delta1_sigma1`); nothing new.

## 🎯 Goal (unchanged)
Discharge `PA_delta1Definable : 𝗣𝗔.Δ₁` and `ISigma1_delta1Definable : 𝗜𝚺₁.Δ₁` by PROVING them
(deliverable `InductionSchemeDelta1.lean`; done-when = file sorry-free). Win = `#print axioms` on both
= `[propext, Classical.choice, Quot.sound]`.

## ⭐ THIS LAP — the keystone is done (the only feasibility-in-doubt piece)
`bv_quote_fixitr : bv (V:=ℕ) ⌜fixitr 0 (fvSup χ) ▹ χ⌝ = fvSup χ` — **proven, axiom-clean.** This pins
the universal-closure arity to `fvSup`, forbidding over-recognition by vacuous leading `∀`s. (Verified
that a mere "body doesn't start with `^∀`" check does NOT suffice — vacuous quantifiers slip through;
the `bv = m` clause is genuinely necessary.) Route: **level-factoring** via `IsSemiformula.sound` +
`subst_comp_fixitr` (NO meta `bvSup` needed). The `≥` direction: a too-small `IsSemiformula j` witness
would re-express `χ = γ ⇜ ![&0…&(j-1)]` with `γ` free-var-free, forcing `fvSup χ ≤ j < fvSup χ`.

Supporting lemmas added this lap (all sorry-free, axiom-clean, committed):
- `Semiterm/Semiformula.quote_castLE`, `.freeVariables_castLE` — `Rew.castLE` preserves raw code & FVs.
- `subst_fvarVec_quote'` — general free-ization `subst (fvarVec m) ⌜β⌝ = ⌜β ⇜ (&·)⌝` for ANY `β:level m`.
- `indBodyVal : V → V` (raw form of `(indBody·).val`) + `indBodyVal_eq` + **`indBodyVal_definable`** (`𝚺₁`).

### 🔑 V=ℕ instance/cast recipe (REUSE in mem_iff — this was the lap's main friction)
Over `V = ℕ` the model uses scoped instances ≠ ℕ-standard:
- Model `NatCast` on ℕ = identity: **`natCast_nat`** (`@Nat.cast ℕ (natCast ℕ) n = n`). `Nat.cast_id` does NOT fire.
- Model `<` on ℕ **IS** `Nat.lt` (ORingStructure extends the existing `LT ℕ`) → after clearing casts,
  `omega` sees `<` directly.
- Model `≤` (`PeanoMinus.instLE = ⟨= ∨ <⟩`) ≠ `Nat.le`: unfold via `(h : a = b ∨ a < b)` ascription (defeq).
  (`le_iff_lt_or_eq` does NOT match — `bv_le` uses the bare scoped `LE`, not the `LinearOrder`'s.)

## 🧱 Recognizer (C = univ) — design FINAL, all atoms verified Σ₁/Δ₁-definable
```
R(p) := ∃ m ≤ p, ∃ b ≤ p, ∃ K ≤ p,
   p = qqAlls b m  ∧  IsUFormula ℒₒᵣ b ∧ shift ℒₒᵣ b = b  ∧  bv ℒₒᵣ b = m
 ∧ IsSemiformula ℒₒᵣ 1 K  ∧  subst (fvarVec m) b = indBodyVal K
```
Definable atoms: `qqAllsDef`, `isUFormula`, `shiftGraph`, `bvGraph`, `isSemiformula`, `substsGraph`,
`fvarVecDef`, `indBodyVal_definable`. `bv b = m` sound by `bv_quote_fixitr`.

## 🎬 NEXT actions (in order — pure mechanical assembly, no open math)
1. **`ch : 𝚫₁.Semisentence 1` + `Defined R ch`** ← START HERE. Build with `.mkDelta (.mkSigma “…”)(.mkPi “…”)`
   in the `“…”` DSL referencing each op's `…Def`/`…Graph` (template: `isSemiformula` in
   `Formula/Basic.lean:1201`, `bv.blueprint`). `indBodyVal` needs a CONCRETE `indBodyValGraph` (the
   `definability` instance is a Prop ∃ — build the graph explicitly OR inline imp/subst/qqAll graphs).
   Prove `Defined R ch` by `.mk` + `simp [the .defined.iff lemmas]`. `Theory.Δ₁.ch` REQUIRES a concrete
   semisentence (Definable is a Prop, not extractable) — this is why ch must be hand-built.
2. **`mem_iff` over ℕ**: `R (⌜φ⌝) ↔ ∃ ψ, φ = univCl(succInd ψ)`. Forward: compose `quote_univCl_eq`,
   `subst_fvarVec_quote`, `indBody_quote`, `bv_quote_fixitr`. Backward: same level-factoring inversion
   already written INSIDE `bv_quote_fixitr` (hcast/hγfree/hχeq) + `subst_fvarVec_quote'` + `IsSemiformula.sound`.
   Use the V=ℕ cast recipe above. Bound `K ≤ p`: need `⌜ψ⌝ ≤ ⌜univCl(succInd ψ)⌝` (code monotonicity).
3. `isDelta1 := ProvablyProperOn.ofProperOn _ (fun V _ _ ↦ Defined.proper)` — FREE.
4. Package `delta1_univ` → **delete dependence; `PA_delta1Definable` axiom-clean**.
5. `delta1_sigma1`: univ core + internal "K is Σ₁" predicate `Cᵢ` (the ONLY remaining unknown-feasibility
   item — **search for an arithmetized `Hierarchy`/`𝚺`-class membership predicate first**, e.g. in
   `Arithmetic/Hierarchy` or the bootstrapped proof machinery, before building one).
6. `Examples.lean`: delete both axioms, `import InductionSchemeDelta1`; verify `#print axioms` clean.

**Laps to finish: ~3 for PA complete (1+2 are the bulk), +1–2 for IΣ₁ (step 5), +0.5 cleanup.**
Risk concentrated only in step 5; steps 1–4,6 are mechanical with every prerequisite proven.

## 📁 Pointers
- `STATUS.md` — living overview + axiom ledger (refreshed this lap).
- `PENDING_WORK.md` — ⭐ recognizer design + DONE/⬜ checklist (bv-pin marked DONE).
- `DIRECTION.md` — frozen route. `Foundation/.../InductionSchemeDelta1.lean` — deliverable.
- Internal API: `Bootstrapping/Syntax/Formula/{Basic,Functions,Coding,Typed}.lean`, `HFS/Vec.lean`.
- Aristotle: poor fit (bespoke Foundation API) — stay local.
