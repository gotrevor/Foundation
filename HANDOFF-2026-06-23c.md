# Handoff: Δ₁-definability — ch ASSEMBLY DONE, closure_inversion PROVEN

**Date**: 2026-06-23 (lap c) · **Branch**: `feat/induction-scheme-delta1` · **HEAD**: see `git log`
**Build**: 🟢 green — `lake build Foundation.FirstOrder.Incompleteness.InductionSchemeDelta1`.
`src/` has exactly **2 sorries**: the PA K-bound and `delta1_sigma1`.

## 🎯 Goal (unchanged)
Discharge `PA_delta1Definable : 𝗣𝗔.Δ₁` and `ISigma1_delta1Definable : 𝗜𝚺₁.Δ₁` by PROVING them.
Win = `#print axioms` on both = `[propext, Classical.choice, Quot.sound]`. `Examples.lean` already
imports this file + the axioms are already deleted (prior lap) — so finishing the 2 sorries finishes
everything.

## ⭐⭐ THIS LAP — the entire recognizer + forward keystone are DONE
Reduced `PA.Δ₁` from one opaque `sorry` to **one isolated code-size bound**. All machine-checked,
axiom-clean, committed (11 commits). Built this lap:
- **`indBodyValGraph`** — concrete `𝚺₁.Semisentence 2` + `via` instance (subst-const absoluteness via
  `Semiterm.quote_eq_encode'`).
- **`chUniv`** — the concrete `𝚫₁.Semisentence 1` recognizer (`.mkDelta` of explicit Σ₁/Π₁ DSL;
  bounded witnesses via `< p+1` since `bexsLT_iff` fires but `≤` has no Hierarchy simp lemma).
- **`InductionUnivR.defined`** — `chUniv` verified as a Δ₁ recognizer over any `V ⊧ IΣ₁` (`simp` resolves
  all sub-graph `Defined.iff`; `eq_comm` canonicalizes Σ/Π orientation; `lt_succ_iff_le` bridges bounds).
- **`delta1_univ` instance** fully wired: `ch := chUniv`, `isDelta1` FREE (from `.defined` properness),
  `mem_iff` via `Defined.iff` → `chUniv_mem_iff`.
- **`chUniv_mem_iff` BACKWARD**: 5/6 clauses + shift (`rew_eq_self_of`) + subst (`subst_fvarVec_quote'`
  + `quote_subst_fvar_fixitr` + `indBodyVal_quote`). Only the K≤p clause is `sorry`.
- **`chUniv_mem_iff` FORWARD**: **FULLY PROVEN**. `IsSemiformula.sound`×2 → `closure_inversion`.
- **`shift-fix`** lemma: `shift β = β ⟹ freeVariables = ∅` via `fvar?_rew` + `Finset.min'` descent.
- **`closure_inversion`** (the hard keystone, whose feasibility was the real doubt): **PROVEN**.
  Works at the CODE level (`⌜·⌝:ℕ` erases the de Bruijn level, sidestepping `0+m` vs `m` casts):
  `⌜fixitr 0 m ▹ χ⌝ = ⌜β⌝` (rebind composite = `castLE` on freevar-free β, `rew_eq_of_funEqOn`);
  `m = χ.fvSup` via `castLE` code-preservation (`Semiformula.quote_castLE`) + `bv_quote_fixitr`.

## 🎬 NEXT — exactly 2 obligations (see PENDING_WORK.md "LAP 2026-06-23c")

### 1. PA K-bound (line ~771) — `⌜ψ⌝ ≤ ⌜univCl'(succInd ψ)⌝` over ℕ. **The one PA blocker.**
- `⌜ψ⌝ ≤ ⌜succInd ψ⌝` is CLEAN: `succInd_eq` ⟹ `∀x !ψ x = ∀⁰ ψ` (identity subst), so `^∀ ⌜ψ⌝` is a
  literal subformula. Use `le_qqAll` + an `q ≤ imp p q` containment (search `le_qqOr`/`nth_le`).
- HARD half: `⌜succInd ψ⌝ ≤ ⌜univCl'⌝ = qqAlls b (0+fvSup)`. KEY FINDING: `^#z = ⟪0,z⟫+1 < ⟪1,z⟫+1 = ^&z`
  (`qqBvar < qqFvar`), so `fixitr` (free→bound) SHRINKS leaves and `subst(fvarVec)` GROWS them — it's a
  size RACE the `fvSup` ∀-wrappers must win. fvSup=0 ⟹ univCl'=χ (equality, clean). fvSup≥1 needs a
  per-∀ growth induction (`x ≤ ^∀ x`) dominating the leaf shrink. Build the new code-size lemmas.
  (Recognizer redesign does NOT dodge it — K≤p, K≤b, K≤s all hit the same `⌜χ⌝ ≤ p` wall.)

### 2. `delta1_sigma1` (line ~817) — IΣ₁ case.
Reuse the univ recognizer + an internal "ψ is `𝚺₁`" predicate `Cᵢ` as an extra `chUniv` clause.
SEARCH the arithmetized `Hierarchy`/`𝚺`-class machinery first (`Arithmetic/Hierarchy*`) before building.

## 📁 Pointers
- `PENDING_WORK.md` — newest 3 sections have the full design + the K-bound size-race analysis + attack paths.
- `DIRECTION.md` — frozen route. Deliverable: `Foundation/.../InductionSchemeDelta1.lean` (done-when target).
- Internal API: `Bootstrapping/Syntax/Formula/{Basic,Functions,Coding,Typed}.lean`; `Basic/Syntax/Rew.lean`
  (fixitr/castLE/rew_eq_*); `Definability/{Hierarchy,Definable}.lean` (mkDelta/graphDelta/Defined.iff).
- All bridge lemmas (`quote_univCl'`, `quote_allClosure`, `bv_quote_fixitr`, `subst_fvarVec_quote'`,
  `quote_subst_fvar_fixitr`, `indBodyVal_quote`, `quote_castLE`) are in-file and proven.
- Aristotle: poor fit (bespoke Foundation API) — stay local.

**Laps to finish: ~1 for PA (the K size-bound), +1–2 for IΣ₁. Hardest piece (closure_inversion) DONE.**
