# mathlib v4.31 forward-port - fork notes & follow-up

Fork-local working notes (gotrevor/Foundation). The v4.31 bump itself **has been offered upstream** (fork PR #1 + email to SnO₂WMaN, 2026-06-21); the deprecation-cleanup TODO below was deliberately kept out of that bump to keep it minimal, and stays fork-side follow-up.

## Done (CI-green on this fork's master)
- **Bump**: `lean-toolchain` + `mathlib` + `doc-gen4` → `v4.31.0`; source migration across 133 `Foundation/*.lean`. `sorry`/`admit` baseline unchanged at 27 (all pre-existing upstream). (commit `Forward-port to Lean/mathlib v4.31.0`)
- **docbuild**: doc-gen4 re-nested into `docbuild/` per doc-gen4's own recommended pattern, out of the main lakefile/manifest. (commit `docs: build doc-gen4 in a nested docbuild/ project`)
- **CI split**: `build` (compile = the gate) vs `docs` (`continue-on-error`, non-blocking) so doc-gen4 breakage never gates a green library. (commit `ci: split compile and doc generation`)

⚠️ All produced with Claude Code, not yet human-reviewed.

## TODO — clean up mathlib v4.31 deprecation warnings
The build is green but emits **122 deprecation-warning lines → 22 distinct symbols** (pure hygiene, no errors). Do this in a **separate worktree**, gated on the lean-universe WIP that makes it trivial. Method: compiler-verified pass (box lap), then CI confirms zero warnings.

### Drop-in renames (~19)
mathlib relation-predicate refactor (the bulk):
- `Reflexive` → `Std.Refl`, `Irreflexive` → `Std.Irrefl`, `Symmetric` → `Std.Symm`, `AntiSymmetric` → `Std.Antisymm`
- `IsRefl` → `Std.Refl`, `IsIrrefl` → `Std.Irrefl`, `IsAsymm` → `Std.Asymm`, `IsAntisymm` → `Std.Antisymm`, `IsTotal` → `Std.Total`, `IsTrichotomous` → `Std.Trichotomous`

Other drop-ins:
- `Set.diff_subset` → `Set.sdiff_subset`, `Set.Infinite.diff` → `Set.Infinite.sdiff`, `Set.insert_diff_singleton` → `Set.insert_sdiff_singleton`
- `List.max?_eq_some_iff'` → `List.max?_eq_some_iff`, `WellFounded.isIrrefl` → `WellFounded.irrefl`, `_root_.not_imp` → `Classical.not_imp`
- 3 deprecated module-imports: `Mathlib.Data.Finite.Card`, `Mathlib.Data.Nat.Lattice`, `Mathlib.Logic.IsEmpty`

### Need per-site care (3)
- `Transitive` → `IsTrans _` — add the type placeholder (Foundation already uses `IsTrans _` elsewhere)
- `Nat.lt_succ` → `Nat.lt_succ_iff` — confirm same statement, not an iff/non-iff swap (4 sites)
- `Std.Irrefl.swap` → `inferInstance` — structural, not a rename (2 sites)

### Possible future optimization
The `docs` CI job rebuilds doc-gen4 from source each run (~27 min). Caching doc-gen4's build would speed it up; non-blocking so not urgent.
