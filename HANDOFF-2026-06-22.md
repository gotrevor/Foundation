# HANDOFF — 2026-06-22 — Δ₁-definability of the induction scheme

**Branch:** `feat/induction-scheme-delta1`  **HEAD:** `c1a77ec`  **Build:** full `Foundation` GREEN.

## Goal (unchanged)
Replace the two `axiom`s with proven instances; win = `#print axioms` on
`PA_delta1Definable` / `ISigma1_delta1Definable` = `[propext, Classical.choice, Quot.sound]`.
Done-when target: `Foundation/FirstOrder/Incompleteness/InductionSchemeDelta1.lean` sorry-free.

## Done this lap (verified)
- **B1 COMPLETE, axiom-clean.** `PeanoMinus.delta1 : 𝗣𝗔⁻.Δ₁ := Theory.Δ₁.ofFinite _ PeanoMinus.finite`
  (`PeanoMinus.finite` already proven upstream). `#print axioms` = the standard three only.
- **Both axioms DELETED** from `Examples.lean`; it now imports `InductionSchemeDelta1` and uses the
  new instances. `#print axioms` on `PA_delta1Definable` / `ISigma1_delta1Definable` shows ONLY
  `sorryAx` + the standard three (no rogue axioms) — verified.
- **PA/IΣ₁ assembled** via `Theory.Δ₁.add PeanoMinus.delta1 <crux>` (defeq to the abbrevs — works).
- Full `Foundation` builds green; no other file references the deleted axiom names.
- Upstream check (local clone `~/src/Foundation` `origin/master`, snapshot 2026-04-15): axioms
  STILL present upstream, no `InductionScheme` Δ₁ instance anywhere — **nothing to port.**

## The ONE remaining obligation (the crux, shared by both instances)
`(InductionScheme ℒₒᵣ C).Δ₁` for `C = Set.univ` (→ PA) and `C = Hierarchy 𝚺 1` (→ IΣ₁).
Two honest `sorry`s in `InductionSchemeDelta1.lean` (`delta1_univ`, `delta1_sigma1`).

**Full design + 3 attack paths + API inventory are in `PENDING_WORK.md`. Read it first.** Summary:
- Recognizer `ch(p) := ∃ q ≤ p, IsSemiformula 1 q ∧ Cᵢ q ∧ inductionAxiom q = p`.
- `inductionAxiom : V → V` is a new `Σ₁` function with `inductionAxiom ⌜ψ⌝ = ⌜univCl (succInd ψ)⌝`.
- **Build it at the TYPED layer** (`Bootstrapping.Semiformula V L n`, `Formula/Typed.lean`): it has
  `all`/`imp`/`subst`/`shift`, and `⌜·⌝` commutes with all of them (`typed_quote_*` `@[simp]`).
  `succIndTyped q = (q.subst ![0]) 🡒 (∀⁰ (q 🡒 q.subst ![#0+1])) 🡒 ∀⁰ q`.
- **THE GAP** (the only deep piece): the internal universal closure `univCl`/`fixitr` (data-dependent
  number of leading `^∀`, free→bound rewrite). No internal counterpart exists; build via the
  `neg`/`shift` `UformulaRec` template + a `qqConj`-style PR recursion for iterated `^∀`.
- `C = Set.univ`: `Cᵢ = ⊤` — **do first**, clears `PA_delta1Definable`.
- `C = Σ 1`: needs internal "ψ is Σ₁" predicate (another arithmetized recursion) — clears IΣ₁.

## Next concrete step
Path A step 1 in a scratch file: define `succIndTyped` and prove
`⌜succInd ψ⌝ = (succIndTyped ⌜ψ⌝).val` (no closure needed — should fall to `typed_quote_*` simp).
Then tackle THE GAP (closure). Aristotle is a poor fit (mathlib-tuned vs. bespoke API) — stay local.

## Housekeeping
- `OUTGOING-COMMENT.md` (untracked): charter web-tools instruction defect — box is network-clamped,
  `WebFetch`/`WebSearch` 60s-timeout; use local Foundation clones for upstream checks, not the web.
- No `ON-LINE-REQUEST.md` open (Hájek–Pudlák ref is nice-to-have, not blocking).
