# HANDOFF — 2026-06-22 — Δ₁-definability of the induction scheme

**Branch:** `feat/induction-scheme-delta1`  **Build:** `Foundation` GREEN (only the 2 crux sorries).

## Goal (unchanged)
Replace the two `axiom`s with proven instances; win = `#print axioms` on
`PA_delta1Definable` / `ISigma1_delta1Definable` = `[propext, Classical.choice, Quot.sound]`.
Done-when target: `Foundation/FirstOrder/Incompleteness/InductionSchemeDelta1.lean` sorry-free.

## Done (verified, committed)
- **B1 axiom-clean**: `PeanoMinus.delta1 : 𝗣𝗔⁻.Δ₁ := Theory.Δ₁.ofFinite _ PeanoMinus.finite`.
- **Both axioms DELETED** from `Examples.lean`; routed through the new instances. `#print axioms`
  shows only `sorryAx` + the standard three on the two headline instances (no rogue axioms).
- **Crux part (a) — internal universal closure — DONE, sorry-free** (in the deliverable file):
  `qqAlls p k = ^∀^[k] p` (Σ₁ via `qqAllsDef`), plus `qqAlls_all`, `qqAlls_succ'`, `le_qqAll`,
  `le_qqAlls`, `isUFormula_qqAlls`, `bv_qqAlls`, `IsSemiformula.qqAlls`, and the headline
  `quote_allClosure : ⌜∀⁰* φ⌝ = qqAlls ⌜φ⌝ n`. This is the reusable hard-won core.
- Upstream FFL (local clone) still ships both as axioms — nothing to port.

## Remaining (the crux) — DECODE plan AVOIDS building `fixitr`
The only open obligations are `delta1_univ` / `delta1_sigma1`. **Read `PENDING_WORK.md`** — full
DECODE design. Key facts (from `Basic/Syntax/Rew.lean`): `univCl'(succInd ψ) = ∀⁰* b`,
`b = fixitr 0 m ▹ succInd ψ`, `m = fvSup ψ`, with `b` **freevar-free** (= internal `IsFVFree`,
already Σ₁ `isFVFreeDef`) and `b ⇜ (fun i ↦ &i) = succInd ψ` (`subst_comp_fixitr`). So the closure
is invertible via the **already-proven internal `subst`** — no `fixitr` needed.

Recognizer: `ch(p) := ∃ m ≤ p, ∃ b ≤ p, p = qqAlls b m ∧ IsFVFree ℒₒᵣ m b ∧`
`  succIndShape (subst (fvarVec m) b) ∧ Cᵢ`.

### Next concrete blocks (each self-contained, template-able)
1. `fvarVec m = ⟨^&0,…,^&(m-1)⟩` — Σ₁ vector fn (template `repeatVec`/`termShiftVec`; recursion
   `fvarVec (m+1) = ^&0 ∷ termShiftVec (len) (fvarVec m)` since `termShift (^&i) = ^&(i+1)`).
   Prove `len`, `nth`, `IsSemitermVec`, and the meta bridge `matrixToVec (fun i:Fin m ↦ ^&↑i) = fvarVec ↑m`.
2. `succIndShape q` (or `succIndCode` forward) — the `succInd` structure: top is
   `imp (q[0]) (imp (^∀ (imp q (q[#0+1]))) (^∀ q))` (`imp a b = neg a ^⋎ b`; meta `imp_eq`). Build at
   the TYPED layer (`Bootstrapping.Semiformula`), correctness by `typed_quote_substs` etc.
3. Side condition `Cᵢ`: `C=univ` → `⊤` (do first, clears PA); `C=Σ1` → internal "ψ is Σ₁" predicate.
4. Assemble `ch`; `mem_iff` (forward via `quote_allClosure`+`subst_comp_fixitr`); `isDelta1`/ProperOn
   (model on `Δ₁Class.defined`). Then `Theory.Δ₁ ⟨ch, mem_iff, isDelta1⟩`.

Aristotle: poor fit (mathlib-tuned vs bespoke API). Stay local.

## Housekeeping
- `OUTGOING-COMMENT.md` (tracked): charter web-tools defect (box network-clamped; use local clones).
- No `ON-LINE-REQUEST.md` open.
